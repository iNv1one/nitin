# Quick Start Guide - Telegram Parser SaaS

## Быстрый старт для тестирования

### 1. Установка зависимостей
```powershell
cd telegram-parser-saas
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Применение миграций
```powershell
python manage.py makemigrations
python manage.py migrate
```

### 3. Инициализация проекта (создание тестовых данных)
```powershell
python manage.py init_project
```

Эта команда создаст:
- Тестового пользователя: `test` / `test123`
- Админа: `admin` / `admin123`
- 2 тестовые группы ключевых слов

### 4. Запуск сервера
```powershell
python manage.py runserver
```

### 5. Открыть в браузере
```
http://localhost:8000/dashboard/
```

Войдите как `test` / `test123`

---

## Что готово для тестирования:

✅ **Модели:**
- User (с подписками и лимитами)
- KeywordGroup (группы ключевых слов)
- MonitoredChat (отслеживаемые чаты)
- ProcessedMessage (обработанные сообщения с CRM)
- BotStatus (статус парсер-бота)

✅ **Веб-интерфейс:**
- Dashboard (главная страница)
- Управление группами ключевых слов
- Управление отслеживаемыми чатами
- Просмотр обработанных сообщений (CRM)
- Настройки пользователя
- Профиль пользователя
- Регистрация/Вход

✅ **Admin панель:**
- Все модели зарегистрированы
- Удобные фильтры и отображение
- Доступ: http://localhost:8000/admin/

✅ **Celery задачи:**
- Обработка сообщений
- Сброс счетчиков (daily, monthly)

---

## Что НЕ работает без дополнительной настройки:

⚠️ **Telegram Parser** - требует:
1. Авторизацию через Telegram API
2. Добавление чатов для мониторинга
3. Запуск парсера: `python -c "from apps.telegram_parser.telegram_client import master_parser; import asyncio; asyncio.run(master_parser.start_monitoring())"`

⚠️ **Celery Worker** - требует Redis:
```powershell
# Установите Redis для Windows или используйте Docker
# Затем запустите:
celery -A config worker -l info
```

⚠️ **AI фильтрация** - требует:
- Валидный OPENAI_API_KEY в .env файле

---

## Минимальный тест без Telegram/Celery:

1. Запустите сервер: `python manage.py runserver`
2. Войдите как `test` / `test123`
3. Протестируйте:
   - ✅ Создание/редактирование групп ключевых слов
   - ✅ Добавление чатов для мониторинга (UI работает, но мониторинг не активен без парсера)
   - ✅ Просмотр настроек
   - ✅ Профиль пользователя
   - ✅ Admin панель (войти как admin / admin123)

---

## Структура проекта

```
telegram-parser-saas/
├── apps/
│   ├── dashboard/          # Веб-интерфейс
│   ├── telegram_parser/    # Парсер и обработка
│   └── users/              # Пользователи
├── config/                 # Настройки Django
├── templates/              # HTML шаблоны
├── static/                 # CSS/JS
├── logs/                   # Логи
├── .env                    # Переменные окружения
└── manage.py
```

---

## Следующие шаги для полноценного тестирования:

1. **Установить Redis** (для Celery)
2. **Настроить Telegram бота** для уведомлений
3. **Авторизовать мастер-парсер** в Telegram
4. **Добавить реальные чаты** для мониторинга
5. **Запустить парсер** и Celery worker

---

Удачного тестирования! 🚀
